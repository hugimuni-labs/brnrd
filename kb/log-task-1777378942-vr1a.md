## [2026-04-28] review | Task Context Bundle v2 follow-up ergonomics evaluation

Created `kb/agent-ergonomics-evaluation/task-context-bundle-v2-followup-review-2026-04-28.md`
from inside the live daemon worktree. The review includes the original event
message verbatim and evaluates context recovery, prompt/docs ergonomics,
stream/task state surfaces, retained worktree wording, per-task log lifecycle,
artifact discoverability, and environment clarity.

Main conclusion: the Task Context Bundle now makes initial orientation quick and
reliable; remaining friction is around runtime state fidelity and artifact
lifecycle. During the review, fixed a scoped stream CLI issue so append-only
stream activity refreshes manifest `updated` timestamps and `brr stream show`
displays current task status from task manifests when available. Updated
`kb/index.md` for the new review and per-task log.

Verification: focused tests passed with `PYTHONPATH=src pytest
tests/test_stream.py tests/test_status_streams.py tests/test_daemon_streams.py
tests/test_runner.py tests/test_daemon.py` (59 passed). A full `PYTHONPATH=src
pytest` run stopped producing progress after `tests/test_adopt.py ...` for
roughly two minutes and was interrupted.
