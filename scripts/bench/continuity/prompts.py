"""The three-session task chain, shared by every contender adapter."""

SESSION_1 = (
    "Add a --json flag to the CLI; we're standardising on snake_case JSON "
    "keys and we never print secrets."
)
SESSION_2 = "Add an export command."
SESSION_3 = (
    "What did we decide about JSON and secrets, and what's still open "
    "from yesterday?"
)
