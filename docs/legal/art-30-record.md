# Article 30 GDPR — Record of Processing Activities

**Prepared for:** HugiMuni SAS, France  
**Service:** brnrd.dev  
**Date:** 2026-07-26  

This is the internal record of processing activities maintained under GDPR Article 30. It describes processing carried out by or on behalf of HugiMuni SAS through the brnrd.dev hosted service.

## A. Controller Processing Activities

HugiMuni SAS is the **data controller** for the following processing activities.

| Activity | Purpose | Data Subjects | Personal Data Categories | Recipients/Sub-processors | Transfer Mechanism | Retention | Security Measures |
|----------|---------|---|---|---|---|---|---|
| Account management | Account creation, authentication, identity persistence | Users (developers, individuals) | GitHub user ID, login | Internal; Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer); GitHub (identity verification, US; DPF/SCCs) | GitHub OAuth 2.0; data stored on Scaleway | Account data retained until account deletion, which is self-service (`POST /v1/accounts/delete`, `src/brnrd/account_deletion.py`): every account- and repo-keyed store is deleted, the `Account` row itself is anonymized in place rather than dropped (its id has to stay valid for the retained billing ledger's foreign key), and any live Stripe subscription is canceled immediately | Password-free via GitHub OAuth; session tokens stored SHA-256-hashed only (`src/brnrd/security.py:12`, `src/brnrd/models.py:63`). **No application-level encryption at rest** — platform-level only (Scaleway/PostgreSQL) |
| Billing and payment | Subscription management, invoice generation, payment processing | Account holders (payers) | Stripe customer ID, billing records (pricing, subscription status, payment events). **No email address** — see note 6: Stripe collects a billing email directly from the payer at checkout; brnrd never receives or stores one | Stripe (payments processor, US; DPF/SCCs; Managed Payments – merchant of record) | Stripe REST API; append-only ledger | Retention not implemented in code; append-only ledger, never pruned. Statutory retention **to be confirmed** with counsel; Stripe retains per its own contract | Card data never reaches brnrd.dev (Stripe Managed Payments, `src/brnrd/stripe_api.py:98-113`); HTTPS in transit |
| Terms acceptance | Contract formation; consent records for the general Terms of Service **and** the hosted-execution beta addendum | Account holders | Account ID, document identifier, version label, SHA-256 of the accepted text, timestamp of acceptance. **No IP address and no user-agent** are recorded: neither is needed to show that this account accepted this text at this time (Art 5(1)(c)) | Internal; Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer) | Stored in the `terms_acceptances` table (`src/brnrd/models.py`), one append-only row per (account, document, version); the general ToS is asked for at login (`web_auth.py`, `/auth/github/callback`), the hosted addendum at the point of use (#664) | Until account deletion (`account_deletion.py` erases these rows). A superseded version's row is **retained** for as long as the account exists, because it is the evidence of the contract in force before the bump | The record carries the SHA-256 of the exact accepted text, and `brnrd.terms.text_for_sha256` reproduces that text from the hash, so an acceptance can be shown rather than merely asserted; a CI test fails if the published page and the pinned text diverge by one word. Rows migrated from the pre-2026-07-26 `accounts.hosted_terms_*` columns carry **no** hash — that text was never pinned and is not recoverable |
| Operational telemetry & service diagnostics | System health, daemon connectivity, quota tracking, feature usage | Users (via daemons) | Account ID, daemon runner/quota information, billing figures, timezone, run counts | Internal; Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer) | Stored on the `Daemon` row (`models.py:189`); wholesale replaced per publish cycle | Replaced on every publish event; the daemon's publisher thread walks every lane on a 3-second loop (`src/brr/gates/cloud_publisher.py:74` `_DASHBOARD_PUBLISH_INTERVAL_S`, corrected 2026-08-18 from an earlier "~25s" figure). Old records discarded. There is no time-based expiry on the columns themselves: a daemon that stops publishing leaves its last snapshot in place until scope narrowing, repo disconnect, or account deletion — `deregister` (`routers/daemons.py:1008`) flips `online` and clears nothing | Database-level access controls |

## B. Processor Processing Activities

HugiMuni SAS is a **data processor** acting on the instructions of user-controllers. Users control the purposes; brnrd relays and stores the content at their direction.

| Activity | Instruction Source | Purpose | Data Subjects | Personal Data Categories | Recipients/Sub-processors | Transfer Mechanism | Retention | Security Measures |
|----------|---|---|---|---|---|---|---|---|
| Message relay (GitHub + Telegram) | User: connects GitHub repositories and/or Telegram bots to daemon | Relay messages between user's chat/issue threads and their locally-running daemon | GitHub: repository collaborators, issue commenters, PR authors; Telegram: chat members | GitHub: author ID, avatar URL, comment text, issue/PR metadata; Telegram: chat ID, user ID, username, message text | GitHub App (US; DPF/SCCs); Telegram bot (UAE; user-elected); Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer) | Events stored in `Event` row (`models.py:347`); `body` holds the message text, `reply_to` (`models.py:357`) the routing identity | Body nulled on user reply (`inbox.py:792`, and the noted-close path `inbox.py:717`); never-answered body nulled at 14d (`inbox.py:808` `_EVENT_BODY_TTL`, applied `inbox.py:834-842`); entire row, routing identity included, deleted at 90d (`inbox.py:809` `_EVENT_ROW_TTL`, applied `inbox.py:833`); sweep runs hourly (`inbox.py:810`) off the activity publish tick (`routers/daemons.py:263`). **These three windows are not the whole retention for message text.** Where the account also publishes the corpus lane's `runs` slice, the same text is mirrored a second time as part of the run node (see the Corpus mirror row below) and lives there under that lane's own bound, not this one | HTTPS; database encryption at rest (platform-level); access logs |
| Corpus mirror & dashboard publishing | User: configures publish scope for brnrd daemon, and — for repos connected on brnrd.dev since this consent step shipped — an explicit per-repo publish-scope consent recorded at connect | Render cache: store verbatim copies of repository pages (code, commit history, issues, etc.) for dashboard display | Repository collaborators, public contributors, any persons named in repo history | Named by lane, because the lane is the unit the user consents to. **Corpus** (`authored` / `knowledge` / `runs` slices) — whole Markdown pages verbatim: the work-surface pages, every knowledge-base page, and per-run `body.md`, `state.md` and the `messages/*.md` transcript of what was said to the agent and what it replied (`src/brr/account.py:1136`, files written `src/brr/message_store.py:149`); the pages routinely quote repository code, so file paths, code, prose, commit authors, issue commenters, PR authors and any third-party data in the repo arrive inside them. **Run ledger** — closed-run receipts: commit subjects, branch names, file paths on the user's own machine, and the agent's prose summary. **Live runs** — the live progress card (`card_text`, unredacted, `schemas.py:722`), run name and label. **Quota** — real spend figures, reset labels carrying the user's timezone, and raw gate error strings. **PR review queue** — PR titles, URLs and author logins. **Runners** — a fingerprint of which agent CLIs are installed and authenticated on the machine. | Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer); GitHub (source pull, read-only API; US; DPF/SCCs) | Corpus in `Account.surface_json` (`models.py:68-73`), written at `routers/daemons.py:381`; the six non-corpus lanes on the `Daemon` row (`models.py:189` ff — `live_runs_json` `models.py:233`, plus `quota_json`, `gate_health_json`, `runners_json`, `pr_review_queue_json`, `run_ledger_json`). Every one is a replaceable render cache, last-write-wins on each publish | Narrowing the server-side publish consent immediately deletes stored lane rows attributable to that repo and corpus slices the account no longer jointly permits (`publish_scope.py:purge_removed_scope`); the other repos' attributable rows and still-permitted slices survive. The full mirror is emptied when the last connected repository is disconnected (`_session.py:786-791`, `surface_json = "[]"`), and on account deletion (`account_deletion.py:238`). **No mirror column carries a time-based retention.** The one clock in this activity is on the producing side and bounds only the `runs` slice: the daemon stops offering run nodes older than `publish.runs_window_days` (default 14, `src/brr/gates/cloud_publisher.py:528`, applied `:575-582`), and the next publish replaces the stored blob without them. The `authored` and `knowledge` slices have **no age bound at all** — every page ships however old, subject only to a 256 KB per-file cap. Where message text also reaches the mirror inside a run node, that window, not the Message relay row's null-after-reply, is its effective retention | HTTPS; database encryption at rest (platform-level); access logs; **two independent controls**: the daemon-local `publish.layers` feature flag, and (repos connected since this consent step shipped) a server-stored per-repo consent (`Repo.publish_layers`, `src/brnrd/publish_scope.py`) enforced again at the `PUT /v1/daemons/*` publish seam — default `none` for a brand-new connect (including repos created through the account API without an explicit scope), and repos carrying no recorded consent at all — those connected before this step existed — are refused every lane by this second control until their owner records a scope; stored values are never backfilled |
| Run content (logs, diffs, ephemeral state) | User: approves each run | Store run outputs (card text, summaries, logs, diffs, build artifacts) transiently for dashboard display | Developers (users and their team members with access to runs) | Card text (user-supplied task descriptions, run status), summaries, file paths, code diffs, log output | Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer) | `card_text` has no column of its own: it is a field inside the `live_runs` lane's snapshot (`schemas.py:722`, stored in `Daemon.live_runs_json`, `models.py:233`, written `routers/daemons.py:603`). `ActivityRecord` stores the summary (`models.py:311`); run bodies themselves ride the corpus lane, one row up. Review packs in RAM only (`pack_relay.py:51`) | Replaced/pruned on subsequent publish; review packs RAM-only with 3600s TTL (`config.py:109` `pack_relay_ttl_s`, swept `pack_relay.py:66-68`); an `ActivityRecord` nothing has reported for 10 minutes is dropped (`activity_records.py:11`, applied `routers/daemons.py:272-276`); `ActivityRecord` pruned on publish; activity data deleted on repo disconnect | HTTPS; database encryption at rest (platform-level); review packs never persisted to disk |
| Router identity routing & channel configuration | User: configures which chat/issue systems route to which daemons | Store mappings between user's external channels (GitHub repos, Telegram chat) and daemon identity (paired_user_id) | Users | Channel IDs, paired user IDs, routing metadata | Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer) | Stored in `ChannelRoute` rows (`models.py:192`) | Deleted on repo disconnect | Database-level access controls |
| Activity record aggregation | System: tracks publishing/query activity for telemetry | Record summary statistics about daemon activity for service diagnostics | Developers (users) | Conversation key, activity summary (prose) | Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer) | Stored in `ActivityRecord` rows (`models.py:128`) | Pruned on publish; deleted on disconnect | Database-level access controls |
| Session token issuance & validation | System: manage authenticated access to dashboard | Issue and validate short-lived access tokens | Account holders using the dashboard | Session token (hashed SHA-256; `models.py:53`), user ID, token creation time | Scaleway SAS (hosting, France, fr-par; intra-EU, no transfer); user's browser (cookie) | Tokens are hashed; cookie has httponly, samesite=lax, secure flags (`web_auth.py:257`); max_age set to SESSION_TTL (30 days, `accounts.py:22`) | 30d session TTL (`accounts.py:22` SESSION_TTL); token deleted on logout | HTTPS-only cookies; SHA-256 hash; httponly + secure flags prevent XSS exfiltration |

## C. Sub-processor Details

| Name | Role | Jurisdiction | Legal Basis for Transfer | DPA/Contract |
|------|------|---|---|---|
| Scaleway SAS | Cloud hosting provider | France (`fr-par`) | Not applicable — processing occurs within the EU/EEA; no international transfer | Scaleway Terms of Service; SLA |
| Stripe | Payment processor | United States | Data Protection Framework (DPF); Standard Contractual Clauses (SCCs) | Stripe Data Processing Addendum |
| GitHub | Identity provider + API partner | United States | Data Protection Framework (DPF); Standard Contractual Clauses (SCCs) | GitHub App terms |
| Telegram | Message transport (user-elected) | United Arab Emirates | User explicitly elects Telegram channel; message transport inherent to service selection | Telegram Terms of Service and Privacy Policy (user responsible) |

## D. Data Subject Rights

Data subjects may exercise the following rights:

- **Access (Art. 15)**: Request copies of personal data by writing security@hugimuni.fr
- **Rectification (Art. 16)**: GitHub identity (login) re-syncs on every login automatically. We hold no email to rectify (see note 6); a billing email is corrected directly with Stripe, at checkout or via the Stripe customer portal. Anything else, by request to security@hugimuni.fr
- **Erasure (Art. 17)**: Self-service account deletion is available from dashboard settings ("danger zone"), confirmed by re-typing the account's GitHub login. It deletes every account- and repo-keyed store (repos, sessions/API keys/daemon tokens, run and activity history, the dashboard mirror, GitHub/Telegram pairings, config-change and runner-wake/stop requests) and cancels any live Stripe subscription immediately; the append-only billing ledger is retained (see §A "Billing and payment" and §F below) and the `Account` row is anonymized rather than dropped, to keep that retained ledger's foreign key valid. Erasure requests outside the self-service path (or for anything it doesn't cover) go to security@hugimuni.fr. Repo-scoped data (ChannelRoute, mirror, ActivityRecord) is also deleted automatically on repo disconnect, independent of full account deletion.
- **Restriction (Art. 18)**: By request to security@hugimuni.fr
- **Portability (Art. 20)**: By request to security@hugimuni.fr
- **Objection (Art. 21)**: By request to security@hugimuni.fr

## E. Data Security Measures

Stated only where verifiable in the code as shipped. Where a control does not
exist, that is said rather than omitted — an inflated security section is a
liability in a document written to reduce it. (Convention borrowed from the
DPA's Annex II, #706.)

- **Encryption in transit**: All traffic over HTTPS. Minimum TLS version is the hosting platform's, not brnrd's — **to be confirmed** with Scaleway rather than asserted here.
- **Encryption at rest**: PostgreSQL database on Scaleway with platform-level encryption
- **Access controls**: platform-level database access controls (Scaleway). No application-level RBAC or audit log exists in `src/brnrd/` — stated as fact, not omitted.
- **Authentication**: GitHub OAuth 2.0 for user identity; session tokens are SHA-256 hashed
- **Tokenization**: Session tokens hashed; card data never stored (Stripe Managed Payments)
- **Incident response**: Contact security@hugimuni.fr for breach reports; internal 72h notification SLA (Art. 33, GDPR)

## F. Notes and Gaps

1. **Account deletion path (Art. 17 gap) — closed 2026-07-25**: Self-service account deletion shipped (`POST /v1/accounts/delete`, `src/brnrd/account_deletion.py`, dashboard "danger zone" button). Manual assistance via security@hugimuni.fr remains for anything the self-service path doesn't cover (see §D).
2. **Operational/pairing rows not individually enumerated above**: `ConfigChangeRequest`, `TgPairCode`, `PairRequest`, `RunnerWakeRequest`, and `RunStopRequest` are account- and/or repo-keyed stores this table's four processing-activity rows don't name individually — they fall under "Account management" in spirit (device-pairing and config-approval handshakes) but weren't previously called out. Noted here rather than silently left for the deletion sweep to discover on its own; all five are covered by the account-deletion path in note 1 above.
3. **Telegram transfer basis**: Transfer to UAE (Telegram's servers) occurs only when user explicitly elects Telegram as an output channel. This is a user-directed transfer, documented in privacy notice.
4. **Review scope**: This record reflects the code state as of 2026-07-24 (commit aaca33d6), updated 2026-07-25 for the Art 17 self-service deletion mechanism (note 1), updated 2026-07-31 for the hosting sub-processor change (note 5), updated 2026-08-16 for the email-collection removal (note 6), and updated 2026-08-18 for the stored-class pass (note 7). Code changes affecting retention times, recipients, or data flows should trigger a review and update of this record.
5. **Hosting sub-processor change — 2026-07-31**: Production hosting moved from Upsun (Platform.sh SAS, Switzerland, `ch-1`) to Scaleway SAS, `fr-par`, France, effective 2026-07-30/31. Every reference to the hosting sub-processor in §A, §B, §C, and §E above has been updated accordingly; the transfer basis for hosting changes from the Switzerland adequacy decision to intra-EU processing, since Scaleway SAS is a French company and hosting now occurs entirely within the EU.
6. **Email collection removed — 2026-08-16 (w-57)**: Maintainer-signed decision to stop collecting a login email. brnrd no longer requests GitHub's `user:email` scope and no longer reads or stores an email address (`src/brnrd/oauth.py`, `src/brnrd/routers/accounts.py`); the "Account management" row's Personal Data Categories cell dropped "email address (nullable)" accordingly. The "Billing and payment" row's forward to Stripe is cut too (`src/brnrd/routers/billing.py::_ensure_customer`): Stripe now collects a billing email directly from the payer at checkout and holds it as its own data, never forwarded by brnrd. The `accounts.email` database column is retained, nullable, and backfilled to NULL for pre-existing rows — a schema detail, not a live processing activity, so it does not reappear in §A. Same edit also fixed **#1418**: the Account management row previously also claimed "name (from GitHub)" as a collected category; the codebase never requests or stores a GitHub display name (only numeric id and login), so that clause was dropped along with email rather than left for #1418 to fix separately — the two overclaims sat in the same table cell.

7. **Stored class named, and stale coordinates repaired — 2026-08-18 (w-63)**: The consent dialog that ships the per-repo publish scope landed in #1453; this is the docs half of the same item. Nothing about the code changed — every edit below makes a cell match a mechanism that was already there.
   - The Corpus row's Personal Data Categories cell said "repository content" and left the reader to guess. It now names the class **by lane**, because the lane is the unit a user actually consents to — and in particular names the two members the old wording hid: the per-run `messages/*.md` transcript of what was said to the agent and what it replied, and the unredacted live `card_text`.
   - **Message text has two retentions, not one.** The Message relay row's three windows (null-after-reply, 14d, 90d) govern the relay queue. Where the account also publishes the corpus lane's `runs` slice, that same text is mirrored a second time inside the run node and lives there under `publish.runs_window_days` (default 14). `SECURITY.md` §"Provenance, and what was not measured" and the `/beta-hosted-execution` page both stated this; this record did not. Both rows now cross-reference.
   - The Corpus row's Retention cell gained what was missing: no mirror column has a time-based retention at all, the `runs` window is producer-side and bounds that slice only, and the `authored` and `knowledge` slices have **no age bound**.
   - **Coordinate rot, repaired.** Six citations in this record pointed at lines that had moved or at columns that do not exist: `Event` was cited as `models.py:158` (it is `:347`); the body-null and the two TTLs as `inbox.py:311/327/328` (they are `:792`, `:808`, `:809`); `pack_relay_ttl_s` as `config.py:62` (it is `:109`); the `Daemon` model as `models.py:70` (that line is `Account.surface_json`; the class is at `:189`); and `card_text` as a `Daemon` column, which has never existed — it is a field inside the `live_runs` snapshot. The publish cadence read "~25s"; the publisher thread's own constant is 3 seconds (`src/brr/gates/cloud_publisher.py:74`). A record whose citations do not resolve cannot be checked by the next reader, which is the whole function of citing them.
   - **Not changed, and referred to the maintainer instead** (this record makes no commitment it did not already make): two fail-open paths this pass found and did not document here, both already acknowledged in their own code docstrings — a daemon token carrying no `repo_id` bypasses the per-lane consent check (`publish_scope.py:411-412`), and corpus slice enforcement returns "nothing to enforce against" for an account with zero connected repos (`publish_scope.py:570-574`). Whether an Article 30 record should carry known gaps of this kind is a call for counsel and the record's named owner, not for a correctness pass.
   - **The warp item's "TG ring buffer" was not written up, because it does not exist.** No ring or circular buffer over chat message bodies appears anywhere in the tree. Telegram retention is the plain `Event` row documented in the Message relay row. The only bounded FIFO is the run-ledger publish snapshot (`deque(maxlen=256)`, `src/brr/gates/cloud_publisher.py:1877`), which is a send-side cap, not a store.

---

**Prepared by**: _(to be signed — this record needs a named human owner before it is relied on)_  
**Last reviewed**: 2026-08-18  
**Next review**: Upon code changes affecting data handling or sub-processor list
