# Screenshot output

Repro drivers write here locally, but generated pixels are ignored. Pull-request
before/after/diff images live in the `shots` branch and are linked from the
`UI screenshots` Actions comment; the workflow is `.github/workflows/ui-shots.yml`.
