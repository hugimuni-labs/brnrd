Generate a concise report on the current state of the repository.  You
have access to `agent_state.md` and may consult `AGENTS.md` for context.

The report should be readable on a phone.  Include:

- Current focus, stated clearly.
- Recent conversation topics with the user — what was discussed, what was
  resolved, what is still open.
- Notable decisions or discoveries since the last report.
- Next steps in priority order.
- Open questions that need resolution.

Write in a direct, professional tone.  Short paragraphs.  Do not
reproduce raw headings from `agent_state.md` — weave the information
into a coherent narrative.

Before finalising, re-evaluate the next steps: are any of them stale,
circular, or heading toward a dead end?  If so, flag them and suggest
a course correction.
